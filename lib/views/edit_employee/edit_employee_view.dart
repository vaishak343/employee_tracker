import 'package:flutter/material.dart';

class EditEmployeeView extends StatelessWidget {
  const EditEmployeeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Edit View"),),
    );
  }
}